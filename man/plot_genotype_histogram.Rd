% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_genotype_histogram.R
\name{plot_genotype_histogram}
\alias{plot_genotype_histogram}
\title{Plot a Histogram of Genotypic Values}
\usage{
plot_genotype_histogram(ans)
}
\arguments{
\item{ans}{A genotype matrix or data frame where:
\itemize{
\item Rows correspond to markers.
\item Columns correspond to individuals.
\item Values are numeric genotypes (\code{0}, \code{1}, \code{2}).
}}
}
\value{
A \code{ggplot2} histogram object.
}
\description{
Generates a histogram of genotypic values (\code{0}, \code{1}, \code{2})
from a genotype matrix and shows the distribution of genotypic classes.
}
\examples{
# Example genotype matrix
geno_matrix <- matrix(
  sample(0:2, 30, replace = TRUE),
  nrow = 10, ncol = 3,
  dimnames = list(paste0("Marker", 1:10), paste0("Ind", 1:3))
)

# Generate and display the histogram
hist_plot <- plot_genotype_histogram(geno_matrix)
print(hist_plot)

}
