% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formater.R
\name{formater}
\alias{formater}
\title{Convert Between Character (A, H, B) and Numeric dosage (0, 1, 2) Formats}
\usage{
formater(geno, numeric_output = TRUE)
}
\arguments{
\item{geno}{A data frame containing genotype data, where values are either
\code{"A"}, \code{"H"}, \code{"B"} or \code{0}, \code{1}, \code{2}. The function assumes the dataset consists
entirely of genotypic or numeric values.}

\item{numeric_output}{Logical. If \code{TRUE}, converts \code{"A"} → \code{0}, \code{"H"} → \code{1}, \code{"B"} → \code{2}.
If \code{FALSE}, converts \code{0} → \code{"A"}, \code{1} → \code{"H"}, \code{2} → \code{"B"}. Default is \code{TRUE}.}
}
\value{
A data frame with the same structure as \code{geno}, but with values converted
based on the \code{numeric_output} parameter.
}
\description{
This function converts genotype data between character (\code{"A"}, \code{"H"}, \code{"B"})
and numeric (\code{0}, \code{1}, \code{2}) formats. The conversion direction is controlled by the
\code{numeric_output} argument.
}
\details{
\itemize{
\item Preserves \code{NA} values during conversion.
\item Unexpected values (anything other than \code{"A"}, \code{"H"}, \code{"B"}, \code{0}, \code{1}, \code{2})
are converted to \code{NA}.
}
}
\examples{
# Example genotype data in character format
geno_char <- data.frame(
  Marker1 = c("A", "H", "B"),
  Marker2 = c("B", "A", "H")
)

# Convert character to numeric format
geno_numeric <- formater(geno_char, numeric_output = TRUE)
print(geno_numeric)

# Convert numeric format back to character format
geno_char_reversed <- formater(geno_numeric, numeric_output = FALSE)
print(geno_char_reversed)

}
