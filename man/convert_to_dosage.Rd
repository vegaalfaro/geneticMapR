% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_to_dosage.R
\name{convert_to_dosage}
\alias{convert_to_dosage}
\title{Convert VCF Genotype Format to Dosage for Diploids}
\usage{
convert_to_dosage(GT)
}
\arguments{
\item{GT}{A character matrix where each entry represents a genotype in VCF format
(\code{"0/0"}, \code{"0/1"}, \code{"1/1"}).}
}
\value{
A numeric matrix of the same dimensions as \code{GT}, where each value represents
the dosage of the alternate allele (\code{0}, \code{1}, or \code{2}).
}
\description{
The function converts a VCF
genotype matrix (with format \code{"0/0"}, \code{"0/1"}, \code{"1/1"})
to allele dosage values (\code{0}, \code{1}, \code{2}) representing the count of the alternative allele.
Mostly used within the package. Check out the more flexible \code{convert_to_dosage_flex} which works for polyploids.
}
\details{
\itemize{
\item Converts the alleles to numeric values and sums them to compute the alternate allele dosage.
\item Retains the original row and column names from the input matrix.
}
}
\examples{
# Example genotype matrix in VCF format
vcf_matrix <- matrix(c("0/0", "0/1", "1/1",
                       "0/1", "0/0", "1/1"),
                     nrow = 2, ncol = 3,
                     dimnames = list(c("Marker1", "Marker2"),
                                     c("Ind1", "Ind2", "Ind3")))

# Convert to dosage
dosage_matrix <- convert_to_dosage(vcf_matrix)
print(dosage_matrix)

}
