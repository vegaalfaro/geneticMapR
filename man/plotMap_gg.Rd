% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotMap_gg.R
\name{plotMap_gg}
\alias{plotMap_gg}
\title{Plot Genetic Map Using ggplot2 in R/qtl data environment}
\usage{
plotMap_gg(map, horizontal = FALSE, color = NULL)
}
\arguments{
\item{map}{\code{cross} object representing a genetic map. The output of R/qtl's \code{read.cross()}, \code{jittermap()} or \code{calc.genoprob()}
\itemize{
\item If a \code{cross} object, the function extracts the map using \code{pull.map()}.
}}

\item{horizontal}{Logical. If \code{TRUE}, the map is plotted with chromosomes arranged horizontally.
Default is \code{FALSE} (vertical layout).}

\item{color}{Character. The color for the marker segments. Default is \code{"red"} if \code{NULL}.}
}
\value{
A \code{ggplot2} object displaying the genetic map.
}
\description{
This function generates a \strong{genetic linkage map} visualization using \href{https://ggplot2.tidyverse.org/}{ggplot2}.
It plots chromosomes as vertical or horizontal lines and displays markers as colored
segments along each chromosome using the data structure built in \href{https://rqtl.org/}{R/qtl}. Replaces \link[qtl]{plotMap} which uses base R.
}
\examples{

# Default plot (vertical)
# plotMap_gg(example_map)

# Horizontal plot with custom color
# plotMap_gg(example_map, horizontal = TRUE, color = "blue")

}
