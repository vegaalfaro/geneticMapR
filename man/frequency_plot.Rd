% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequency_plot.R
\name{frequency_plot}
\alias{frequency_plot}
\title{Generate a Histogram of Genotype Frequencies}
\usage{
frequency_plot(freq_df)
}
\arguments{
\item{freq_df}{Usually the output of \code{freq}. A data frame where rows represent markers or individuals,
and columns represent genotype categories with their frequencies.}
}
\value{
A \code{ggplot2} histogram visualizing the distribution of genotype frequencies.
The processed long-format data is attached as an attribute (\code{attr(output, "data")}).
}
\description{
The function creates a histogram of genotype frequencies for each genotype category
(e.g., \verb{"0", "1", "2"}) based on a frequency data frame. The function also
returns the processed long-format data as an attribute. It will not work if your data
is in A, H, B format. Use \code{formater} to change to dosage before using \code{frequency_plot.}
}
\details{
\itemize{
\item Converts the input data frame to long format using \code{pivot_longer()}.
\item Ensures correct ordering of genotype categories.
\item Generates a faceted histogram where each panel represents a genotype category.
\item Stores both the generated plot and the processed data but returns only the plot by default.
}
}
\examples{
# Example frequency data frame
freq_data <- data.frame(
  `0` = c(0.2, 0.3, 0.4),
  `1` = c(0.5, 0.4, 0.4),
  `2` = c(0.3, 0.3, 0.2),
  row.names = c("Marker1", "Marker2", "Marker3")
)

# Generate the frequency histogram
p <- frequency_plot(freq_data)
print(p)  # Display the plot

# Access the processed long-format data
attr(p, "data")

}
