% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_qtl_trace.R
\name{plot_qtl_trace}
\alias{plot_qtl_trace}
\title{Generates a line plot showing QTL traces (LOD scores) across chromosomes for one or more traits.
Supports customization of visual appearance and annotations such as significance thresholds and vertical lines.}
\usage{
plot_qtl_trace(
  qtl_df,
  thresholds_df,
  vline_df = NULL,
  use_physical_pos = FALSE,
  x_angle = 0,
  trait_colors = NULL,
  trait_labels = NULL,
  x_label = "Position (cM)",
  y_label = "LOD",
  plot_title = NULL,
  show_legend = TRUE,
  facet_nrow = 1
)
}
\arguments{
\item{qtl_df}{A data frame containing QTL mapping results. Must include columns: \code{pos} (genetic position in cM),
\code{lod} (LOD score), \code{response_var} (trait name), and \code{chr} (chromosome). Optional \code{phys.pos} (physical position in Mb).}

\item{thresholds_df}{A data frame with LOD score thresholds per trait. Must include columns: \code{chr}, \code{hline}, and \code{response_var}.}

\item{vline_df}{Optional. A data frame specifying positions for vertical lines. Must include \code{chr} and \code{vline} columns.
Can also include a \code{label} column for text annotation at each vertical line.}

\item{use_physical_pos}{Logical. If TRUE, use physical position (\code{phys.pos}) for the x-axis. If FALSE (default), use genetic position (\code{pos}).}

\item{x_angle}{Integer. Angle of the x-axis text labels (default is 0).}

\item{trait_colors}{Optional. A named vector of colors for each trait. If NULL (default), colors are automatically assigned.}

\item{trait_labels}{Optional. A named vector of human-readable trait labels (can include expressions) to use in the legend.}

\item{x_label}{Character. Label for the x-axis (default is \code{"Position (cM)"}).}

\item{y_label}{Character. Label for the y-axis (default is \code{"LOD"}).}

\item{plot_title}{Optional. Title for the plot (default is NULL).}

\item{show_legend}{Logical. Whether to display the legend (defaults to TRUE).}

\item{facet_nrow}{Integer. Number of rows in the facet layout (defaults to 1).}
}
\value{
A \code{ggplot} object displaying QTL traces per trait and chromosome, with optional custom styling.
}
\description{
Generates a line plot showing QTL traces (LOD scores) across chromosomes for one or more traits.
Supports customization of visual appearance and annotations such as significance thresholds and vertical lines.
}
